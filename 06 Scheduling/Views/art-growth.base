filters:
  and:
    - file.hasTag("art")
views:
  - type: tasknotesKanban
    name: Creation Pipeline
    groupBy:
      property: status
      direction: ASC
    order:
      - status
      - scheduled
    sort:
      - property: scheduled
        direction: ASC
    options:
      columnWidth: 280
      hideEmptyColumns: false
